// import styles from "./CollectionPage.module.css";

import { Collection } from "@widgets";

export const CollectionPage = ({}) => {
  return (
    <div className={"page"}>
      <Collection />
    </div>
  );
};
